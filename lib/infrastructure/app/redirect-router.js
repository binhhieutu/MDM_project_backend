const express = require("express");
const router = express.Router();

// Admin

// router.get("/admin/home-menu/list", async (req, res) => res.redirect("/admin/home-menu"));

module.exports = router;
